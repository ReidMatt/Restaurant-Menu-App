<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu Alerts</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <h1>Menu Alerts</h1>
    <% if (alerts.length > 0) { %>
    <ul>
      <% alerts.forEach(alert => { %>
      <li><strong><%= alert.restaurant %></strong>: <%= alert.message %></li>
    <% }); %>
    </ul>
    <% } else { %>
    <p>No current alerts for any restaurant.</p>
  <% } %>
  <a href="/">Back to Home</a>
  
  </body>
</html>